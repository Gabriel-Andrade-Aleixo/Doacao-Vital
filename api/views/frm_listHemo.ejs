<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        Gerenciar Hemocentros
    </title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <link rel="icon" type="image/x-icon" href="imagens/logofav.ico">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Tilt+Neon&display=swap" rel="stylesheet">

<style>
    header {
        background-color: rgb(189, 90, 57);
        padding: 5px;
        color: white;
        text-align: center;
        width: 100%;
        font-family: "Roboto", serif;
        font-weight: 100;
        font-style: normal;
    }

    header img {
        height: 90px;
    }

    header h1{
        font-family: "Roboto", serif;
        font-weight: 300;
        font-style: normal;
        color: rgb(237, 237, 209);
    }

    h1{
        font-family: "Roboto", serif;
        font-weight: 300;
        font-style: normal;
    }
 
    .navbar {
        display: flex;
        justify-content: center;
    }

    .navbar a {
        color: white;
        text-decoration: none;
        margin: 0 15px;
        font-size: 1.1rem;
    }

    .navbar a:hover {
        color: #ffcccb;
    }

    h1{
        font-family: "Roboto", serif;
        font-weight: 300;
        font-style: normal;
    }

    body {
        background-color: rgb(237, 237, 209);
        font-family: "Roboto", serif;
        font-weight: 100;
        font-style: normal;
    }

    .logoff{
        display: flex;
        justify-content: center;
        background-color: rgb(189, 90, 57);
        color: white;
        border: none;
        padding: 12px;
        width: 100%;
        border-radius: 5px;
        cursor: pointer;
        font-size: 18px;
        font-weight: bold;
        transition: background-color 0.3s ease, transform 0.3s ease;

    }

    .logoff:hover {
        background-color: #f35c3e;
        transform: translateY(-2px);
    }

    .logoff:active {
        background-color: #d84315;
        transform: translateY(2px);
    }
</style>
</head>

<body>
    <header>
        <figure>
            <img id="logo" src="./imagens/Gota.png" alt="Logo Projeto do Hemocentro">
        </figure>
        
        <h1 id="h1-site">Doação Vital</h1>
        <br>
        <form action="/sairConta" method="get" onsubmit="return confirm('Tem certeza que deseja sair da conta?')">
            <button type="submit" class="logoff">Sair da conta</button>
        </form>
    </header>
    <h1>Lista Usuários</h1>
    <div>
        <fieldset>
            <legend>Registros de Usuários</legend>
            <table class="table">
                <tr>
                    <th>ID</th>
                    <th>Nome</th>
                    <th>Cidade</th>
                    <th>Bairro</th>
                    <th>Rua</th>
                    <th>Número</th>
                    <th>Telefone</th>
                </tr>
                <% for (let i=0; i <obj_hemo.length; i++) { %>
                    <tr>
                        <td>
                            <%= obj_hemo[i].id_hemocentro %>
                        </td>
                        <td>
                            <%= obj_hemo[i].nome_hemocentro %>
                        </td>
                        <td>
                            <%= obj_hemo[i].cidade_hemo %>
                        </td>
                        <td>
                            <%= obj_hemo[i].bairro_hemo %>
                        </td>
                        <td>
                            <%= obj_hemo[i].rua_hemo %>
                        </td>
                        <td>
                            <%= obj_hemo[i].numero_hemo %>
                        </td>
                        <td>
                            <%= obj_hemo[i].telefone_hemocentro %>
                        </td>

                        <td>
                            <button onclick="deletarHemocentro('<%= obj_hemo[i].id_hemocentro%>')"
                                class="btn btn-danger">X</button>
                        </td>
                    </tr>
                    <% } %>
            </table>
        </fieldset>
    </div>

    <script>
        function deletarHemocentro(id_hemocentro) {
            if (confirm("Certeza?")) {
                fetch(`/listarHemocentro/${id_hemocentro}`, {
                    method: "DELETE"
                })
                .then(response => {
                    if(response.ok){
                        alert("Deletado")
                        location.reload()
                    }
                    else {
                        alert("Erro")
                    }
                })
                .catch(error => console.errpr("Erro:", error));
            }
        }
    </script>
</body>

</html>